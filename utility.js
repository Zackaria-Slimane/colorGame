export function randomValue(value) {
	return Math.floor(Math.random() * value)
}

export function randomRanges(low, high) {
	return Math.floor(Math.random() * high - low + 1)
}

//TODO
//Formatter
//Difficulty - config the formatter

//*Function on page load , change format or change difficulty
//get a formatter
//configure formatter accodring to difficulty
//generate colors and render
//handle clicking a color

//generate correct color
//generate similar color
